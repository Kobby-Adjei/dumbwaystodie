{
  "name": "@dumbways/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Shared typed protocol for Dumb Ways to Die. Source of truth for all payload shapes.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "test": "node --test test/*.test.js"
  }
}
