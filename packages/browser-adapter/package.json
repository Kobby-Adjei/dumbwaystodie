{
  "name": "@dumbways/browser-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Chromium extension that pairs one chat tab and drives it, degrading to the clipboard path when it cannot.",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@dumbways/protocol": "*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@types/chrome": "^0.0.280"
  }
}
