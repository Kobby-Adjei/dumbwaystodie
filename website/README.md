# DWTD website

Static landing page for Dumb Ways to Die. No build step or remote assets.

Preview from the repository root:

```sh
python3 -m http.server 4173 --directory website
```

Then open `http://127.0.0.1:4173/`.
